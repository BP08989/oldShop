{% extends 'base.html.twig' %}

{% block body %}
    <h1>Configurableproduct</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ configurableProduct.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurableproduct_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurableproduct_edit', { 'id': configurableProduct.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
